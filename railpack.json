{
  "$schema": "https://schema.railpack.com/latest/schema.json",
  "packages": {
    "python": "3.12"
  },
  "steps": {
    "install": {
      "commands": [
        "npm install",
        "python -m pip install --break-system-packages --no-cache-dir plotly==5.24.1 kaleido==0.2.1"
      ]
    }
  }
}

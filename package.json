{
  "name": "farmranger-tag-bot",
  "version": "1.0.0",
  "description": "Lightweight Telegram front end for tagexplore-web: reads one organisation's tag-discovery data from the web app's API and reports new discoveries. Does no log scraping itself.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "postinstall": "node scripts/install-python-deps.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-telegram-bot-api": "^0.66.0"
  },
  "engines": {
    "node": ">=18"
  }
}
